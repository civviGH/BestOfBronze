check for high elo

read db
shuffle lines

for every line
  check if summoner id is ingame
  if ingame and ranked game:
    read summoner ids of all 10 players
    for every id: check elo, if low enough, add to db
    check time
    if time < constraint:
      
  if not:
    check next summoner


